name: Codesign Windows App

on:
  push:
    branches: [main]
    # tags:
    # - "v*"

jobs:
  build:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v3

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: "14.18.1"

      - name: Install dependencies
        run: npm install -g @

      - name: Build Windows app
        uses: samuelmeuli/action-electron-builder@v1
        with:
          build_script_name: "electron:windows-actions"
          github_token: ${{ secrets.github_token }}
          release: ${{ startsWith(github.ref, 'refs/tags/v') }}

      # - name: Codesign exe
      #   run: |
      #     echo ${{ secrets.CODESIGN }} | base64 -d > codesign.pfx
      #     pwsh -Command "& '.\codesign.ps1' -cert .\codesign.pfx -password '${{ secrets.PASSWORD }}' -path .\dist_electron\win-unpacked"
      #     mv .\dist_electron\win-unpacked\my-app.exe .\dist_electron\win-unpacked\latest.yml

      #   windows_certs: ${{ secrets.CODESIGN }}
      #   windows_certs_password: ${{ secrets.PASSWORD }}
